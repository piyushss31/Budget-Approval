jFreeChart Library is used.

Other Standard Library which are used are included in the project Resources Folder.

First Change the path of the database according to the system in Sqlite.java class

Run LoginFrame.java to run the project